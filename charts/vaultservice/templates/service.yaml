apiVersion: v1
kind: Service
metadata:
  name: {{ include "vaultservice.fullname" . }}
  labels:
    {{- include "vaultservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vaultservice.selectorLabels" . | nindent 4 }}
